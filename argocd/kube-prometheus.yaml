apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monitoring-kube-prometheus
  namespace: argocd-prd
spec:
  generators:
  - list:
      elements:
      - env: production
  template:
    metadata:
      name: 'monitoring'
    spec:
      project: default
      source:
        repoURL: https://github.com/infrastructure.git
        targetRevision: 'main'
        path: 'monitoring/kube-prometheus'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'monitoring'
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
